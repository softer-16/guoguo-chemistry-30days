# 果果的化学项目规则

- 商品为完整30天课程的一次买断授权。
- 有效授权后，Day01–Day30必须全部立即可访问。
- `plan_start_date`只用于学习建议和日历提醒，不得用于锁课。
- 不得恢复`max_day=10`或任何等价的分阶段访问限制。
- 不得把`service_role`、数据库密码或其他高权限密钥放入前端。
- 每次只推进一个经用户确认的Task，不预先开始下一Task。
- 线上数据库操作、push、合并、部署和删除数据必须暂停，等待用户明确确认。
